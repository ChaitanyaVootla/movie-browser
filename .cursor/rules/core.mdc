---
description: Quick reference for critical rules - links to detailed rule files
globs: ["src/**/*.ts", "src/**/*.tsx"]
alwaysApply: true
---

# Movie Browser - Quick Reference

This file contains critical "Do NOT" rules and links to detailed documentation.
For in-depth guidance, refer to the specific rule files below.

## Rule Files Index

| File | Description |
|------|-------------|
| **architecture.mdc** | Project overview, tech stack, directory structure, critical coding rules |
| **database.mdc** | PostgreSQL migration, hydration service, data sources, YouTube engagement |
| **routing-loading.mdc** | Routing patterns, progressive loading, hero image architecture |
| **rsc-optimization.mdc** | RSC payload optimization, Light DTOs, what NOT to trim |
| **discover-topics.mdc** | Browse/Topics architecture, URL params, watch options |
| **state-mobile.mdc** | Zustand state management, mobile patterns, responsive layout |
| **analytics.mdc** | ClickHouse + Grafana analytics, tracking, admin dashboard |

## Do NOT

### Framework & Architecture
- Import from `@/components/ui/` and modify those files (shadcn components)
- Use `getServerSideProps` or `getStaticProps` (App Router only)
- Create API routes for simple CRUD (use Server Actions)
- Skip TypeScript types
- Use inline styles (use Tailwind)
- Add client components without explicit justification

### React Patterns
- Use `useEffect` to reset state on prop changes (use React keys instead)
- Use non-deterministic functions in render (Intl.NumberFormat, Date.now, Math.random)

### Images
- Bypass the CDN for images (always use `src/lib/image.ts` utilities)

### Layout
- Put sidebar in normal document flow on desktop (use fixed positioning)

### Mobile
- Use `touch-pan-x` on scrollers (blocks vertical page scrolling on mobile)

### Data Fetching
- Use raw `fetch()` for TMDB API calls (bypasses retry logic - use `fetchFromTMDB`)

## Critical Patterns

### Server Components First

Default to Server Components. Only add "use client" when you need:
- Event handlers (onClick, onChange, etc.)
- Hooks (useState, useEffect, custom hooks)
- Browser APIs (window, localStorage)
- Third-party client libraries

### Naming Conventions

- **Files**: kebab-case (`movie-card.tsx`, `use-auth.ts`)
- **Components**: PascalCase (`MovieCard`, `AuthProvider`)
- **Hooks**: camelCase with `use` prefix (`useAuth`, `useMovieData`)
- **Server Actions**: camelCase (`getMovie`, `addToWatchlist`)
- **Types/Interfaces**: PascalCase (`Movie`, `UserSession`)
- **Constants**: SCREAMING_SNAKE_CASE (`API_BASE_URL`)

### Import Order

```tsx
// 1. React/Next.js
import { Suspense } from "react";
import Image from "next/image";

// 2. External packages
import { motion } from "framer-motion";

// 3. Internal components
import { Button } from "@/components/ui/button";
import { MovieCard } from "@/components/features/movie/movie-card";

// 4. Hooks/stores
import { useUserStore } from "@/stores/user";

// 5. Utils/lib
import { cn } from "@/lib/utils";
import { getPosterSources } from "@/lib/image";

// 6. Types
import type { Movie } from "@/types";
```

### Structured Logging

```typescript
import { dataLogger, apiLogger, aiToolLogger } from "@/lib/logger";

// GOOD - Structured logging
dataLogger.error({
  event: "fetch_movie_error",
  movieId: id,
  error: error instanceof Error ? error.message : String(error),
});

// BAD - Unstructured console.error
console.error("Failed to fetch movie:", error);
```

### CDN Image Pattern

```tsx
import { getPosterSources } from "@/lib/image";

const sources = getPosterSources(item, "movie");
// primary: https://image.themoviebrowser.com/movie/{id}/poster.webp
// fallback: https://image.tmdb.org/t/p/w500/...
```

### TMDB API Calls (with retry)

```typescript
import { fetchFromTMDB } from "@/server/services/tmdb";

// Always use the central service for retry logic
const data = await fetchFromTMDB<MovieData>(`/movie/${id}`, {
  params: { append_to_response: "credits,videos" },
  cacheNamespace: "movie",
});
```

### Touch Scrolling

```tsx
// GOOD: Allows both horizontal scroll AND vertical page scroll
<div className="flex overflow-x-auto touch-manipulation">

// BAD: Blocks vertical page scrolling when touch starts on scroller
<div className="flex overflow-x-auto touch-pan-x">
```

### Flex Container Alignment (Hero Content)

```tsx
// GOOD: items-start prevents children from stretching
<div className="flex flex-col items-start gap-2.5">
  <RatingsBar ... />  {/* Will fit content width */}
</div>

// BAD: Without items-start, flex children stretch to fill width
<div className="flex flex-col gap-2.5">
  <RatingsBar ... />  {/* Will stretch to full container width */}
</div>
```

## Quick Links

- **Tech Stack**: See `architecture.mdc`
- **Database Queries**: See `database.mdc`
- **Progressive Loading**: See `routing-loading.mdc`
- **RSC Optimization**: See `rsc-optimization.mdc`
- **URL Parameters**: See `discover-topics.mdc`
- **Zustand Stores**: See `state-mobile.mdc`
- **Analytics Tracking**: See `analytics.mdc`
